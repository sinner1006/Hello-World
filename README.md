# Hello World Mobile App

A simple Expo React Native app that displays **Hello, World!** each time the app is opened.

## Run locally

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the app:
   ```bash
   npm run start
   ```
3. Open on a device/simulator via Expo.
